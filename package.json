{
  "name": "webrtc",
  "version": "0.0.1",
  "description": "WebRTC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -e js, json"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "node-static": "^0.7.10",
    "nodemon": "^2.0.15",
    "socket.io": "^2.0.4"
  }
}
